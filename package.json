{
  "dependencies": {
    "axios": "^1.7.7",
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start:parcel": "npx parcel index.html",
    "start:db": "json-server --watch db.json"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "process": "^0.11.10"
  }
}
